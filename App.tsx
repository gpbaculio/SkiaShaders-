import React from 'react';

import {AppContainer} from 'src/components';
import SkiaShaders from 'src/SkiaShaders';

const App = () => {
  return (
    <AppContainer>
      <SkiaShaders />
    </AppContainer>
  );
};

export default App;
